# AI健康管理系统 - 技术演进方案 v3.0

## 一、项目概述

### 1.1 项目背景
AI健康管理系统（AI-Health-System）是一个基于大语言模型的智能健康管理平台，已具备Agent编排、模型路由、向量检索、订阅计费等核心能力。本方案在现有系统基础上，从五个维度进行技术演进，提升系统的家庭共享能力、长程记忆能力、跨时区适配能力、多模态输入能力和隐私保护能力。

### 1.2 技术栈
- **后端**: Java Spring Boot + MyBatis-Plus
- **数据库**: MySQL + Redis + RocketMQ + Qdrant向量数据库
- **前端**: Soybean-Admin (Vue 3)
- **核心能力**: Agent编排、多模型路由、RAG向量检索、订阅计费

### 1.3 演进目标
| 演进方向 | 核心目标 | 关键技术 |
|---------|---------|---------|
| Family Link | 家庭组共享架构 | 级联鉴权、数据隔离、Redis缓存 |
| Long-Term Memory | AI长程记忆与交叉分析 | 三层记忆架构、时间衰减RRF |
| Chronos Scheduler | 动态时区自适应调度 | Redis ZSet延迟队列、时区转换 |
| Zero-Effort Ingestion | 拍照语音多模态输入 | Vision模型、Function Calling |
| Privacy Vault | 隐私数据控制台与物理焚毁 | 物理擦除、记忆沙盒、审计日志 |

---

## 二、家庭组共享架构 (Family Link)

### 2.1 设计理念
通过家庭组实现订阅共享和数据协同，同时保证底层健康数据的隐私隔离。非主账号仅能查看汇总周报，无法访问脱敏体征明细。

### 2.2 数据库设计

#### 2.2.1 家庭表 (sys_family)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键 |
| family_name | VARCHAR(100) | 家庭名称 |
| family_avatar | VARCHAR(255) | 家庭头像 |
| creator_id | BIGINT | 创建人ID |
| subscription_id | BIGINT | 关联订阅ID |
| max_members | INT | 最大成员数（默认6） |
| share_health_data | TINYINT | 是否共享健康数据 0=否 1=是 |
| share_reports | TINYINT | 是否共享报告 0=否 1=是 |
| status | TINYINT | 状态 0=禁用 1=正常 |
| created_at/updated_at | DATETIME | 时间戳 |

#### 2.2.2 家庭成员关系表 (sys_family_user)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键 |
| family_id | BIGINT | 家庭ID |
| user_id | BIGINT | 用户ID |
| member_role | VARCHAR(20) | 角色：OWNER/ADMIN/MEMBER/CHILD/ELDER |
| nickname_in_family | VARCHAR(50) | 家庭内昵称 |
| data_visibility | VARCHAR(20) | 数据可见性：PRIVATE/FAMILY/REPORT_ONLY |
| can_view_members | TINYINT | 是否可查看成员列表 |
| join_time | DATETIME | 加入时间 |
| invited_by | BIGINT | 邀请人ID |
| status | TINYINT | 状态 |

#### 2.2.3 家庭邀请表 (sys_family_invitation)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键 |
| family_id | BIGINT | 家庭ID |
| inviter_id | BIGINT | 邀请人ID |
| invitee_phone/email | VARCHAR | 被邀请人联系方式 |
| invite_code | VARCHAR(20) | 邀请码（8位随机） |
| member_role | VARCHAR(20) | 成员角色 |
| expire_time | DATETIME | 过期时间（7天） |
| status | TINYINT | 状态：0=待接受 1=已接受 2=已过期 |
| accepted_user_id | BIGINT | 接受用户ID |
| accepted_at | DATETIME | 接受时间 |

### 2.3 核心功能

#### 2.3.1 订阅级联鉴权 (SubscriptionAspect改造)
```
请求 → SubscriptionAspect切面
    ↓
1. 检查个人订阅权限
    ↓ 不满足
2. 检查是否为家庭/企业版功能（family/enterprise）
    ↓ 是
3. 查询用户是否属于有有效家庭订阅的群组（Redis缓存30分钟）
    ↓ 有
4. 放行请求，共享家庭配额
```

**关键实现**:
- 切面增加 `FamilyService` 和 `StringRedisTemplate` 注入
- 新增 `hasFamilySubscriptionAccessWithCache()` 方法，带30分钟Redis缓存
- 新增 `isFamilyShareableTier()` 判断是否支持家庭共享
- 仅 `family` 和 `enterprise` 等级支持共享，`pro` 及以下不支持

#### 2.3.2 数据隔离策略
- **强隔离**: 非主账号无法访问底层体征明细（血糖、血压等原始数据）
- **汇总共享**: 家庭成员可查看动态周报（由定时任务生成汇总）
- **可配置**: 家庭主账号可配置数据共享开关（share_health_data）
- **多租户**: Mapper层引入家庭ID过滤，确保数据安全

### 2.4 接口设计
| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/v1/family/create | 创建家庭组 |
| GET | /api/v1/family/my-families | 获取我的家庭列表 |
| GET | /api/v1/family/primary | 获取主家庭信息 |
| GET | /api/v1/family/{familyId}/members | 获取家庭成员列表 |
| POST | /api/v1/family/{familyId}/invite | 邀请成员 |
| POST | /api/v1/family/join | 通过邀请码加入 |
| DELETE | /api/v1/family/{familyId}/members/{memberId} | 移除成员 |
| PUT | /api/v1/family/{familyId}/members/{memberId}/role | 更新成员角色 |
| POST | /api/v1/family/{familyId}/leave | 退出家庭 |

---

## 三、AI长程记忆与交叉分析 (Long-Term Memory)

### 3.1 设计理念
将用户记忆分为三层，结合时间衰减的向量检索算法，实现AI对用户健康历史的长期记忆和交叉联想能力。

### 3.2 三层记忆架构

| 层级 | 名称 | 特点 | 衰减速度 | 示例 |
|------|------|------|---------|------|
| L1 | 瞬时记忆 (Session) | 会话级上下文，对话结束后清理 | 快（24小时） | 当前对话内容、临时问题 |
| L2 | 核心画像 (Profile) | 用户硬性条件，几乎不衰减 | 极慢 | 慢性病史、过敏源、年龄性别 |
| L3 | 时间线线索 (Timeline) | 健康事件演进，按时间衰减 | 中（14天半衰期） | 胃痛发作、运动记录、饮食打卡 |

### 3.3 数据库扩展

#### 3.3.1 用户记忆表扩展 (user_memory)
新增字段：
- `memory_layer`: 记忆层级（SESSION/PROFILE/TIMELINE）
- `event_time`: 事件发生时间
- `decay_rate`: 衰减系数
- `tags`: 标签（逗号分隔）

#### 3.3.2 健康事件时间线表 (health_event_timeline)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键 |
| user_id | BIGINT | 用户ID |
| event_type | VARCHAR(30) | 事件类型：SYMPTOM/DIAGNOSIS/MEDICATION/SURGERY/LIFESTYLE_CHANGE/CHECKUP |
| event_title | VARCHAR(200) | 事件标题 |
| event_description | TEXT | 事件详细描述 |
| event_date | DATE | 事件发生日期 |
| severity | VARCHAR(20) | 严重程度：MILD/MODERATE/SEVERE |
| related_indicators | JSON | 相关指标 |
| source | VARCHAR(30) | 来源：USER_INPUT/AI_EXTRACTED/MEDICAL_RECORD |
| is_verified | TINYINT | 是否已验证 |
| embedding | TEXT | 向量表示（1536维） |

### 3.4 时间衰减RRF融合算法

#### 3.4.1 算法原理
在标准RRF（倒数排名融合）基础上，引入时间衰减因子，让近期的记忆和记录获得更高权重。

**时间衰减公式**:
```
decay = exp(-λ * days)
```
- λ: 衰减系数（默认0.05，约14天半衰期）
- days: 距离现在的天数

**最终得分公式**:
```
finalScore = normalizedRrf * (1 - timeWeight) + decay * timeWeight
```
- timeWeight: 时间衰减权重（默认0.3，即30%）

#### 3.4.2 混合检索流程
```
用户查询 → 三路检索
    ├─ 向量语义检索 → 排名1
    ├─ 时间排序检索 → 排名2
    └─ 重要性/访问频率 → 排名3
    ↓
RRF融合 + 时间衰减加权
    ↓
返回TopK结果
```

### 3.5 记忆上下文构建
AI对话时自动构建记忆上下文：
```
【用户核心画像】
- 2型糖尿病患者，确诊于2023年
- 青霉素过敏
- 高血压，日常服用硝苯地平

【近期健康记忆】
- [DIET] 午餐吃了红烧肉和米饭 (2024-01-15)
- [SYMPTOM] 晚上胃痛，持续约1小时 (2024-01-14)
- [EXERCISE] 晨跑3公里，感觉良好 (2024-01-13)

【健康事件时间线（近30天）】
- 2024-01-10 [CHECKUP] 年度体检，血糖偏高
- 2024-01-05 [MEDICATION] 开始服用二甲双胍
```

### 3.6 接口设计
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/v1/memory/profile | 获取核心画像记忆 |
| GET | /api/v1/memory/timeline | 获取时间线记忆（带衰减排序） |
| POST | /api/v1/memory/add | 添加用户记忆 |
| GET | /api/v1/memory/context | 获取AI记忆上下文 |
| POST | /api/v1/memory/health-event | 添加健康事件 |
| GET | /api/v1/memory/health-events | 获取健康事件时间线 |
| POST | /api/v1/memory/search | 混合检索记忆 |

---

## 四、动态时区自适应调度 (Chronos Scheduler)

### 4.1 设计理念
废除基于服务器固定时区的@Scheduled强定时策略，改用Redis ZSet延迟队列，根据用户所在时区动态计算提醒时间，彻底解决跨时区提醒错乱问题。

### 4.2 核心架构

```
┌─────────────────────────────────────────────────────────┐
│                    Chronos Scheduler                     │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  每天凌晨2点（服务器时间）                                │
│         ↓                                               │
│  遍历活跃用户 → 获取用户时区 → 计算当地时间对应服务器时间戳 │
│         ↓                                               │
│  存入 Redis ZSet (Score = 触发时间戳)                    │
│                                                         │
│  后台常驻线程（每秒轮询）                                 │
│         ↓                                               │
│  取出到期任务 → 发送 RocketMQ → WebSocket推送            │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 4.3 数据库设计

#### 4.3.1 用户配置扩展
- `user_profile` 表新增：
  - `timezone_id`: 时区ID（如 Asia/Shanghai）
  - `last_locate_time`: 最后定位时间
  - `latitude/longitude`: 经纬度
  - `locale`: 语言区域

#### 4.3.2 用户提醒时间表 (user_reminder_schedule)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键 |
| user_id | BIGINT | 用户ID |
| reminder_type | VARCHAR(30) | 提醒类型：CHECKIN/WATER/EXERCISE/SLEEP/MEAL/MEDICATION |
| reminder_time | VARCHAR(10) | 提醒时间 HH:mm（用户当地时间） |
| repeat_days | VARCHAR(50) | 重复星期（1-7，逗号分隔） |
| is_enabled | TINYINT | 是否启用 |
| snooze_minutes | INT | 贪睡时长 |
| vibration_only | TINYINT | 仅震动 |
| custom_message | VARCHAR(200) | 自定义消息 |

### 4.4 核心实现

#### 4.4.1 Redis ZSet延迟队列
- **ZSet Key**: `chronos:reminder:zset`
- **Member**: 任务ID（userId:type:time:timestamp）
- **Score**: 触发时间戳（毫秒）
- **任务详情**: `chronos:reminder:task:{taskId}`（String类型，48小时过期）

#### 4.4.2 时区转换工具
```java
// 用户当地时间 → 服务器时间戳
long timestamp = TimezoneUtils.toServerTimestamp(userLocalTime, userTimezone);

// 计算下次提醒时间（今天已过则明天）
long nextTrigger = TimezoneUtils.calculateNextReminderTimestamp("08:00", "America/New_York");
```

#### 4.4.3 调度流程
1. **每日重建**: 每天凌晨2点，分批处理所有活跃用户（500/批）
2. **单用户调度**: 获取用户时区 → 遍历提醒配置 → 计算服务器时间戳 → 存入ZSet
3. **轮询触发**: 守护线程每秒轮询ZSet，取出到期任务发送MQ
4. **消费推送**: MQ消费端通过WebSocket推送给用户

### 4.5 默认提醒配置
| 类型 | 时间 | 说明 |
|------|------|------|
| 打卡提醒 | 08:00 | 每日健康打卡 |
| 饮水提醒 | 10:30, 15:00, 19:00 | 每日三次饮水提醒 |
| 运动提醒 | 20:00 | 晚间运动+睡眠准备 |

---

## 五、拍照与语音多模态极简输入 (Zero-Effort Ingestion)

### 5.1 设计理念
利用Vision大模型和语音识别，让用户通过拍照、说话就能完成健康数据录入，实现零手动输入的极简体验。

### 5.2 核心流程

#### 5.2.1 图片输入流程
```
用户拍午餐照 → 上传图片 → 存入OSS
    ↓
发送多模态处理任务到MQ
    ↓
ModelRouter路由到Vision模型（Qwen-VL/DeepSeek-VL）
    ↓
NutritionAgent解析图片 → 识别食物、估算分量、计算热量
    ↓
Function Calling自动组装 DietRecordSubmitDTO
    ↓
调用 FoodService 自动入库
    ↓
返回解析结果给用户确认/修正
```

#### 5.2.2 语音输入流程
```
用户说"我今天中午吃了红烧肉"
    ↓
ASR语音转文字
    ↓
LLM意图识别 + 实体抽取
    ↓
Function Calling自动组装对应DTO
    ↓
调用对应Service自动入库
```

### 5.3 数据库设计

#### 5.3.1 多模态上传记录表 (multimodal_upload)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键 |
| user_id | BIGINT | 用户ID |
| upload_type | VARCHAR(20) | 上传类型：IMAGE/AUDIO/VIDEO |
| file_url | VARCHAR(500) | 文件存储URL |
| file_size | BIGINT | 文件大小（字节） |
| mime_type | VARCHAR(100) | MIME类型 |
| duration_seconds | INT | 时长（秒） |
| processing_status | VARCHAR(20) | 处理状态：PENDING/PROCESSING/SUCCESS/FAILED |
| processing_result | JSON | AI解析结果 |
| record_type | VARCHAR(30) | 关联记录类型：DIET/EXERCISE/SYMPTOM |
| record_id | BIGINT | 关联记录ID |
| model_used | VARCHAR(50) | 使用的模型 |
| confidence | DECIMAL(3,2) | 解析置信度 |
| user_corrected | TINYINT | 用户是否修正过 |
| error_message | TEXT | 错误信息 |
| created_at | DATETIME | 创建时间 |
| processed_at | DATETIME | 处理完成时间 |

### 5.4 ModelRouter多模态路由扩展
- **新增模型配置**: Qwen-VL-Max、DeepSeek-VL 等视觉模型
- **路由策略**: 根据输入类型自动选择
  - 纯文本 → 标准LLM（DeepSeek/Qwen/GLM）
  - 图片+文本 → Vision模型（Qwen-VL）
  - 语音 → ASR + LLM

### 5.5 Function Calling编排
在AgentOrchestrator中新增函数调用能力：

**可用函数**:
- `create_diet_record(foods, meal_type, meal_time)` - 创建饮食记录
- `create_exercise_record(type, duration, intensity)` - 创建运动记录
- `create_symptom_record(symptoms, severity, duration)` - 创建症状记录
- `create_blood_sugar_record(value, measure_time, note)` - 创建血糖记录

### 5.6 接口设计
| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/v1/multimodal/upload | 上传多模态文件 |
| GET | /api/v1/multimodal/{uploadId} | 获取处理结果 |
| PUT | /api/v1/multimodal/{uploadId}/correct | 用户修正解析结果 |
| GET | /api/v1/multimodal/history | 获取上传历史 |

---

## 六、隐私数据控制台与物理焚毁 (Privacy Vault)

### 6.1 设计理念
提供完善的隐私数据管理能力，包括数据授权、物理焚毁、AI记忆沙盒、数据导出等功能，满足GDPR等隐私合规要求。

### 6.2 核心功能

#### 6.2.1 物理数据焚毁
用户可一键清除所有个人数据，不仅是MySQL数据，还包括向量索引、缓存、对话历史等。

**焚毁范围**:
- ✅ MySQL健康记录（血糖、饮食、运动等）
- ✅ Qdrant向量索引（deletePoints物理删除）
- ✅ Redis缓存（用户相关所有Key）
- ✅ AI记忆数据（UserMemory + HealthEventTimeline）
- ✅ 对话历史和AI调用日志
- ✅ 多模态上传文件

**执行方式**:
- 异步执行（RocketMQ）
- 操作审计日志
- 完成后通知用户

#### 6.2.2 AI记忆沙盒开关
用户可随时关闭AI记忆功能，关闭后：
- 对话内容不写入AiCallAuditLog
- 不更新用户记忆（UserMemory）
- 不更新健康事件时间线
- 向量索引不新增
- 但仍可使用AI功能（无记忆模式）

**实现方式**:
- `sys_user` 表新增 `ai_memory_enabled` 和 `do_not_track` 字段
- 请求上下文注入do-not-track标记
- AI调用切面检查标记，决定是否记录

#### 6.2.3 数据导出
用户可申请导出所有个人数据，支持多种导出类型：
- **FULL**: 全量数据导出
- **HEALTH**: 仅健康数据
- **MEMORY**: 仅AI记忆数据
- **ACTIVITY**: 仅活动记录

**导出格式**: JSON + CSV，打包为ZIP，7天内可下载

### 6.3 数据库设计

#### 6.3.1 隐私审计日志表 (privacy_audit_log)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键 |
| user_id | BIGINT | 用户ID |
| action_type | VARCHAR(50) | 操作类型：DATA_EXPORT/DATA_PURGE/CONSENT_CHANGE/MEMORY_SANDBOX |
| action_description | VARCHAR(200) | 操作描述 |
| action_details | JSON | 操作详情 |
| ip_address | VARCHAR(50) | 操作IP |
| user_agent | VARCHAR(500) | 操作设备 |
| result | VARCHAR(20) | 结果：SUCCESS/FAILED |
| error_message | TEXT | 错误信息 |
| created_at | DATETIME | 创建时间 |

#### 6.3.2 数据导出任务表 (data_export_task)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键 |
| user_id | BIGINT | 用户ID |
| export_type | VARCHAR(20) | 导出类型：FULL/HEALTH/MEMORY/ACTIVITY |
| export_scope | JSON | 导出范围（时间范围等） |
| status | VARCHAR(20) | 状态：PENDING/PROCESSING/COMPLETED/FAILED |
| file_url | VARCHAR(500) | 导出文件URL |
| file_size | BIGINT | 文件大小 |
| record_count | INT | 记录数量 |
| error_message | TEXT | 错误信息 |
| expire_at | DATETIME | 过期时间 |
| created_at | DATETIME | 创建时间 |
| completed_at | DATETIME | 完成时间 |

#### 6.3.3 用户表扩展
- `sys_user` 表新增：
  - `ai_memory_enabled`: AI记忆是否开启（默认1）
  - `do_not_track`: 是否不追踪模式（默认0）
  - `data_retention_days`: 数据保留天数（默认365）

### 6.4 前端隐私合规可视化
在 `views/settings/privacy/index.vue` 页面展示：
- 📊 **数据存储统计**: 动态图表展示各类加密记录数量
- 🔒 **加密状态**: AES-256加密标识
- 🎛️ **AI记忆沙盒开关**: 一键开关，实时生效
- 📋 **操作审计日志**: 所有隐私操作可追溯
- 📤 **数据导出**: 一键申请导出个人数据
- 🔥 **物理焚毁**: 安全确认后一键清除所有数据

### 6.5 接口设计
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/v1/privacy/consent | 查询数据授权状态 |
| PUT | /api/v1/privacy/consent | 更新数据授权状态 |
| POST | /api/v1/privacy/purge | 提交数据物理焚毁 |
| GET | /api/v1/privacy/memory-sandbox | 获取AI记忆沙盒状态 |
| PUT | /api/v1/privacy/memory-sandbox | 切换AI记忆沙盒开关 |
| POST | /api/v1/privacy/export | 请求数据导出 |
| GET | /api/v1/privacy/export/{taskId} | 获取导出任务状态 |
| GET | /api/v1/privacy/export/list | 获取导出任务列表 |
| GET | /api/v1/privacy/statistics | 获取隐私数据统计 |
| GET | /api/v1/privacy/audit-logs | 获取隐私操作审计日志 |

---

## 七、技术实现清单

### 7.1 已完成的后端文件

#### 实体类 (Entity)
- ✅ `SysFamily.java` - 家庭组实体
- ✅ `SysFamilyUser.java` - 家庭成员关系实体
- ✅ `SysFamilyInvitation.java` - 家庭邀请实体
- ✅ `HealthEventTimeline.java` - 健康事件时间线实体
- ✅ `UserReminderSchedule.java` - 用户提醒配置实体
- ✅ `MultimodalUpload.java` - 多模态上传记录实体
- ✅ `PrivacyAuditLog.java` - 隐私审计日志实体
- ✅ `DataExportTask.java` - 数据导出任务实体

#### Mapper接口
- ✅ `SysFamilyMapper.java`
- ✅ `SysFamilyUserMapper.java`
- ✅ `SysFamilyInvitationMapper.java`
- ✅ `HealthEventTimelineMapper.java`
- ✅ `UserReminderScheduleMapper.java`
- ✅ `MultimodalUploadMapper.java`
- ✅ `PrivacyAuditLogMapper.java`
- ✅ `DataExportTaskMapper.java`

#### Service层
- ✅ `FamilyService.java` + `FamilyServiceImpl.java` - 家庭组服务
- ✅ `UserMemoryService.java` + `UserMemoryServiceImpl.java` - 用户记忆服务
- ✅ `MultimodalService.java` + `MultimodalServiceImpl.java` - 多模态服务
- ✅ `PrivacyService.java` + `PrivacyServiceImpl.java` - 隐私服务（扩展）

#### 核心组件
- ✅ `TimeDecayRrfFusion.java` - 时间衰减RRF融合算法
- ✅ `TimezoneUtils.java` - 时区工具类
- ✅ `ChronosScheduler.java` - 动态时区调度器
- ✅ `SubscriptionAspect.java` - 订阅级联鉴权切面（改造）

#### Controller层
- ✅ `FamilyController.java` - 家庭组控制器
- ✅ `MemoryController.java` - 用户记忆控制器
- ✅ `PrivacyController.java` - 隐私控制器（扩展）

#### 数据库脚本
- ✅ `migration_phase3_family_privacy.sql` - 完整数据库迁移脚本

### 7.2 MQ主题扩展
- ✅ `HEALTH_REMINDER` - 健康提醒推送
- ✅ `MULTIMODAL_PROCESS` - 多模态处理任务
- ✅ `PRIVACY_DATA_PURGE` - 隐私数据焚毁任务

---

## 八、部署与测试建议

### 8.1 部署顺序
1. **数据库迁移**: 执行 `migration_phase3_family_privacy.sql`
2. **配置更新**: 更新RocketMQ主题配置
3. **后端部署**: 部署新的Service和Controller
4. **缓存预热**: 家庭订阅缓存、DNT缓存
5. **调度器启动**: ChronosScheduler自动启动

### 8.2 测试要点

#### 家庭组测试
- [ ] 创建家庭组、邀请成员、加入家庭
- [ ] 家庭订阅级联鉴权（个人无订阅但家庭有）
- [ ] 成员角色权限（OWNER/ADMIN/MEMBER）
- [ ] 数据隔离验证（非主账号无法查看明细）

#### 长程记忆测试
- [ ] 三层记忆的增删查
- [ ] 时间衰减排序正确性
- [ ] 混合检索结果合理性
- [ ] 记忆上下文构建完整性

#### 时区调度测试
- [ ] 多时区用户提醒时间正确性
- [ ] Redis ZSet任务添加和触发
- [ ] 跨天提醒计算正确性
- [ ] 批量调度性能

#### 多模态测试
- [ ] 图片上传和解析流程
- [ ] 语音转文字和意图识别
- [ ] 自动创建记录准确性
- [ ] 用户修正功能

#### 隐私功能测试
- [ ] AI记忆沙盒开关效果
- [ ] 数据物理焚毁完整性（MySQL+Qdrant+Redis）
- [ ] 数据导出功能
- [ ] 审计日志记录完整性

### 8.3 性能优化建议
1. **家庭订阅缓存**: 30分钟Redis缓存，减轻切面压力
2. **DNT状态缓存**: 24小时Redis缓存，快速判断是否追踪
3. **批量调度**: 每天凌晨分批处理，避免数据库压力
4. **异步处理**: 多模态解析、数据焚毁全部走MQ异步

---

## 九、后续演进方向

### 9.1 短期优化
- 完善多模态模型接入（Qwen-VL真实调用）
- 增加家庭组数据共享的细粒度控制
- 优化时间衰减参数的个性化配置

### 9.2 中期规划
- 家庭健康排行榜和互动功能
- AI记忆的自动摘要和提炼
- 多语言/多时区的完整国际化
- 移动端拍照识别的端侧优化

### 9.3 长期愿景
- 家庭健康画像的代际分析
- 多模态健康数据的深度融合
- 隐私计算（联邦学习）支持
- 医疗级数据合规认证

---

## 十、总结

本技术演进方案从五个维度对AI健康管理系统进行了全面升级：

1. **家庭组共享** - 扩展了订阅模式，提升用户粘性和付费转化率
2. **长程记忆** - 增强了AI的个性化和上下文理解能力
3. **动态时区** - 解决了全球化用户的提醒错乱问题
4. **多模态输入** - 大幅降低了用户录入数据的门槛
5. **隐私保护** - 满足合规要求，增强用户信任感

这些演进不仅提升了产品的核心竞争力，也为后续的商业化和全球化奠定了坚实的技术基础。

---

**文档版本**: v3.0
**最后更新**: 2026-07-01
**适用项目**: AI-Health-System
