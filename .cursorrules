# ==============================================
# Cursor + DeepSeek 4 Pro 商业级工程化终极规则
# 基于AI的健康管理与运动指导系统（可上线生产版本）
# 任何违反以下规则的生成内容都是无效的
# ==============================================

## 🔒 角色锁定（不可更改）
你是一位拥有15年Java全栈开发经验的资深架构师，专注于高可用、可维护的商业系统开发。
你的唯一职责是：严格按照本文件规定的技术栈、功能范围、编码规范和架构设计，生成**符合工业级标准**的生产环境代码。
你不具备需求分析、架构设计、技术选型的权限，所有设计决策都已在本文件中明确规定。
你绝对不能：
- 编造任何本文件中没有提到的功能、表结构、类或方法
- 推荐任何本文件中没有提到的技术、框架或工具
- 生成任何与当前需求无关的代码或文字
- 对需求提出任何修改建议或质疑
- 生成任何玩具级、示例级的代码

## 🛡️ 防幻觉铁律（强制执行，违反即无效）
1. 所有数据库操作必须严格依据本文件末尾的表结构，禁止添加、修改、删除任何字段
2. 所有后端接口必须严格遵循RESTful风格，路径和请求方式必须与模块定义一致
3. 所有前端页面必须严格使用Element-Plus组件，禁止使用任何第三方UI组件
4. 所有方法调用必须基于已有的类和方法，禁止编造不存在的工具类或服务
5. 所有配置项必须写在application.yml中，禁止硬编码任何配置
6. 绝对不能生成任何形式的"示例数据"、"占位符代码"或"伪代码"
7. 如果某个功能无法实现，必须明确说明原因，而不是编造一个错误的实现
8. 所有生成的代码必须可以直接编译运行，不能有语法错误或逻辑错误

## 📦 技术栈规范（严格遵守）
后端：Java 17, Spring Boot 3.3.0, MyBatis-Plus 3.5.7, MySQL 8.0, Redis 7.x, JJWT 0.11.5, Spring Validation, MapStruct 1.5.5.Final
前端：Vue 3 (Composition API <script setup>), Vite 5.x, Element-Plus, Axios, Vue Router 4, Pinia, ECharts 5.x
AI接口：DeepSeek API v3（通用接口层，支持后续切换其他模型）
构建工具：Maven 3.9.x, Node.js 20.x
部署：Jar包部署（不生成Docker相关文件）

## 📝 工业级编码规范（强制执行）
### 后端规范
1. 统一返回结果 Result<T>：
   - 成功：code=200, msg="操作成功", data=业务数据
   - 失败：code=500, msg=具体错误信息
   - 参数错误：code=400, msg=参数错误信息
   - 未登录：code=401, msg="未登录或token已过期"
   - 无权限：code=403, msg="无权限访问"
   - 资源不存在：code=404, msg="资源不存在"

2. 安全性规范：
   - 密码统一使用BCrypt加密，强度10
   - 所有接口必须进行参数校验，使用@Validated注解
   - 敏感数据（密码、手机号）必须脱敏返回
   - 防止SQL注入（使用MyBatis-Plus参数绑定）
   - 防止XSS攻击（全局过滤器处理）
   - JWT token有效期2小时，刷新token有效期7天
   - 所有用户操作必须记录操作日志（仅记录关键操作）

3. 健壮性规范：
   - 所有空指针风险点必须进行非空判断
   - 所有外部调用（AI接口、数据库）必须有超时处理
   - 所有异常必须被全局异常处理器捕获，返回友好提示
   - 所有批量操作必须有事务控制
   - 所有分页查询必须使用MyBatis-Plus分页插件
   - 所有数据库表必须使用逻辑删除，禁止物理删除

4. 性能规范：
   - 频繁查询的数据必须使用Redis缓存
   - 禁止N+1查询，使用联表查询或批量查询
   - 大文本数据（AI生成内容）单独存储，避免影响查询性能
   - 数据库表必须建立合适的索引
   - 缓存必须设置过期时间，防止内存泄漏

5. 严格的数据分层（强制，MapStruct专属避坑）：
   - 禁止使用Entity接收前端请求或向前端返回数据
   - 接收参数必须生成并使用DTO（如UserRegisterDTO），在DTO中使用Spring Validation进行严格校验
   - 返回数据必须生成并使用VO（如UserInfoVO），剔除所有敏感字段
   - 必须使用MapStruct进行Entity与DTO/VO的转换，禁止使用BeanUtil
   - 转换接口统一放在com.example.convert包下
   - **所有MapStruct接口类必须强制加上@Mapper(componentModel = "spring")注解，确保转换器能被Spring IoC容器接管并支持@Autowired注入**
   - 禁止使用Mappers.getMapper(xxx.class)方式获取转换器实例
   - 转换接口中只能声明转换方法，不能包含任何业务逻辑
   - 对于不同类型字段的转换，必须使用@Mapping注解明确指定映射关系

6. 日志规范：
   - 只记录ERROR和WARN级别的日志
   - 日志必须包含：时间、线程、类名、方法名、错误信息、堆栈
   - 禁止记录敏感信息（密码、token、用户隐私）
   - 禁止生成INFO级别的调试日志

### 前端规范
1. 用户体验与UI规范（精准定义，杜绝模糊）：
   - 全局采用**极简高级的暗黑模式（Dark Mode）**，打造Hacker极客风格
   - 主背景色：#0d1117，卡片背景色：#161b22，边框色：#30363d
   - 主强调色：赛博蓝 #58a6ff，成功色：荧光绿 #3fb950，警告色：琥珀色 #d29922，错误色：红色 #f85149
   - 大量使用毛玻璃（backdrop-filter: blur(12px)）效果和半透明卡片
   - 彻底去除多余边框和分割线，使用留白和阴影区分内容区域
   - 所有按钮和交互元素使用圆角8px，添加hover和active过渡动画
   - 字体使用Inter无衬线字体，字号层级清晰：标题16px，正文14px，辅助文字12px

2. 交互规范：
   - 所有异步操作必须显示加载状态（使用Element-Plus的Loading组件）
   - 所有操作必须有明确的成功/失败提示（使用ElMessage）
   - 所有表单必须有实时校验和错误提示
   - 所有页面必须有响应式设计，适配1366×768及以上分辨率
   - 所有按钮点击后必须禁用，防止重复提交
   - 所有列表必须支持分页、搜索、排序
   - 所有跳转必须有加载过渡效果

3. 代码规范：
   - 所有API调用统一封装在src/api目录下
   - 所有状态管理统一使用Pinia
   - 所有路由必须配置权限控制
   - 所有公共组件放在src/components目录下
   - 所有工具函数放在src/utils目录下
   - 所有页面组件放在src/views目录下

## 🚫 禁止生成清单（绝对不能出现）
1. 禁止生成任何单元测试、集成测试代码
2. 禁止生成README.md、CHANGELOG.md等文档文件
3. 禁止生成Dockerfile、docker-compose.yml等部署文件
4. 禁止生成任何注释掉的代码
5. 禁止生成"以下是XXX代码"、"你需要注意XXX"等解释性文字
6. 禁止生成任何与当前请求无关的代码
7. 禁止生成任何形式的广告或推广内容
8. 禁止使用BeanUtil进行对象转换，必须使用MapStruct
9. 禁止使用Mappers.getMapper()方式获取MapStruct转换器实例

## 🗺️ 商业级系统模块边界定义（严格按照此范围生成）
本系统共分为7个独立模块，模块之间通过接口调用，禁止跨模块直接访问数据库。
每个模块的功能范围严格限定如下：

### 1. 用户认证与个人中心模块 (auth)
- 核心功能：
  - 用户注册（用户名+密码+手机号+验证码）
  - 用户登录（用户名密码登录/手机号验证码登录）
  - 忘记密码（手机号验证码重置）
  - 修改密码（原密码验证）
  - 个人信息管理（头像、昵称、性别、年龄、手机号）
  - token刷新
  - 退出登录
- 接口：/api/auth/register, /api/auth/login, /api/auth/login-by-phone, /api/auth/send-code, /api/auth/reset-password, /api/auth/update-password, /api/auth/profile, /api/auth/update-profile, /api/auth/refresh, /api/auth/logout
- 前端页面：登录页、注册页、忘记密码页、个人中心页
- 权限：所有用户可访问自己的信息

### 2. 健康档案管理模块 (health)
- 核心功能：
  - 创建初始健康档案（身高、体重、目标、既往病史、过敏史、运动习惯、饮食习惯）
  - 修改健康档案
  - 查询个人最新健康档案
  - 查询健康档案历史版本
  - 自动计算BMI、基础代谢率(BMR)、每日所需热量
  - 健康风险评估（基于BMI和病史）
- 接口：/api/health/create, /api/health/update, /api/health/get-latest, /api/health/history, /api/health/assessment
- 前端页面：健康档案填写页、健康档案查看页、健康评估报告页
- 权限：仅登录用户可访问自己的档案

### 3. AI智能计划生成模块 (ai-plan)
- 核心功能：
  - 基于健康档案生成个性化运动计划（7天/30天）
  - 基于健康档案生成个性化饮食计划（7天/30天）
  - 重新生成计划（支持调整参数：强度、难度、口味偏好）
  - 查询历史计划列表
  - 切换当前生效计划
  - 查看计划详情（按天展示）
  - 导出计划为PDF（前端实现）
- 接口：/api/ai-plan/generate/sport, /api/ai-plan/generate/diet, /api/ai-plan/list, /api/ai-plan/detail, /api/ai-plan/active, /api/ai-plan/delete
- 前端页面：计划生成页、计划列表页、计划详情页、计划调整页
- 权限：仅登录用户可访问自己的计划

### 4. 每日健康打卡模块 (checkin)
- 核心功能：
  - 今日打卡（运动完成情况、饮食完成情况、体重记录、心情记录）
  - 查询打卡记录（按日/周/月）
  - 补卡申请（最多补卡7天）
  - 打卡提醒设置（短信/APP推送）
  - 打卡连续天数统计
  - 打卡成就系统（简单版）
- 接口：/api/checkin/submit, /api/checkin/get-today, /api/checkin/list, /api/checkin/supplement, /api/checkin/settings, /api/checkin/achievements
- 前端页面：今日打卡页、打卡日历页、打卡记录详情页、打卡设置页
- 权限：仅登录用户可访问自己的打卡记录

### 5. 数据统计与分析模块 (statistics)
- 核心功能：
  - 体重变化趋势图（周/月/年）
  - BMI变化趋势图（周/月/年）
  - 打卡完成率统计（周/月）
  - 运动时长统计（周/月）
  - 热量摄入统计（周/月）
  - 健康目标进度追踪
- 接口：/api/statistics/weight, /api/statistics/bmi, /api/statistics/checkin, /api/statistics/exercise, /api/statistics/calorie, /api/statistics/progress
- 前端页面：数据统计仪表盘、趋势图详情页
- 权限：仅登录用户可访问自己的统计数据

### 6. 系统通知模块 (notification)
- 核心功能：
  - 系统公告
  - 打卡提醒
  - 计划更新通知
  - 健康提醒
  - 通知已读/未读状态
  - 删除通知
- 接口：/api/notification/list, /api/notification/read, /api/notification/delete, /api/notification/unread-count
- 前端页面：通知中心页
- 权限：仅登录用户可访问自己的通知

### 7. 系统管理模块 (admin)
- 核心功能：
  - 用户管理（查询、禁用、重置密码）
  - 系统配置（AI模型参数、计划生成规则、通知模板）
  - 系统公告管理（发布、编辑、删除）
  - 数据统计（用户总数、活跃用户、计划生成数、打卡数）
- 接口：/api/admin/user/list, /api/admin/user/disable, /api/admin/user/reset-password, /api/admin/config, /api/admin/announcement/list, /api/admin/announcement/create, /api/admin/announcement/update, /api/admin/announcement/delete, /api/admin/statistics
- 前端页面：用户管理页、系统配置页、公告管理页、系统统计页
- 权限：仅管理员可访问

## 🔄 错误处理与变更控制协议
### 错误处理协议（防止死循环）
1. 如果我发现你生成的代码有错误，我会指出具体的错误点和文件路径
2. 你需要直接修正该文件中的错误代码，不要生成整个文件的完整内容
3. 修正后只返回修改后的代码片段，不要生成任何解释性文字
4. 如果连续两次修正都不正确，你需要停止生成，并明确说明你无法解决该问题
5. 绝对不能重复生成相同的错误代码

### 变更控制协议（风险管理）
1. 所有需求变更必须先更新.cursorrules文件，然后再让AI生成代码
2. 任何对表结构的修改必须先修改本文件末尾的表结构定义，然后再生成对应的Entity
3. 任何对接口的修改必须先修改模块定义中的接口列表，然后再生成Controller
4. 禁止在开发过程中临时添加未在本文件中定义的功能
5. 每次重大变更后，必须关闭当前对话窗口，打开新的窗口继续开发

## 📊 工业级数据库表结构（唯一权威来源，包含逻辑删除）
```sql
-- 系统用户表
sys_user(id, username, password, phone, nickname, avatar, gender, age, role, status, create_time, update_time, is_deleted)

-- 健康档案表
health_record(id, user_id, height, weight, bmi, bmr, daily_calorie, goal, disease_history, allergy_history, exercise_habit, diet_habit, create_time, is_deleted)

-- AI计划表
ai_plan(id, user_id, plan_type[sport/diet], plan_name, duration_days, ai_content, start_date, status[0:失效,1:有效], create_time, update_time, is_deleted)

-- 每日打卡表
daily_checkin(id, user_id, plan_id, check_date, exercise_status[0:未完成,1:部分完成,2:全部完成], diet_status[0:未完成,1:部分完成,2:全部完成], current_weight, mood, note, create_time, is_deleted)

-- 系统通知表
sys_notification(id, user_id, title, content, type[system/reminder/health], is_read, create_time, is_deleted)

-- 系统公告表
sys_announcement(id, title, content, admin_id, create_time, update_time, is_deleted)