# Production environment
# Relative path — browser sends /api/* to same host, nginx proxies to backend
VITE_SERVICE_BASE_URL=/api
# No proxy in production (nginx handles it)
VITE_HTTP_PROXY=N
VITE_SOURCE_MAP=N
